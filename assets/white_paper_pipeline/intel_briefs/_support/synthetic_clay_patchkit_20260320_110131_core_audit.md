# Synthetic Clay Patchkit

Status: proposed controlled-English constraints only.  
These are not implementation proof and were not executed in this run.

## Proposed Patches By Basin

- A. Operator-facing prose must say Adam unless the exact shell/history distinction is necessary for provenance.
- B. A memode assertion is admissible only if every member is behavioral and support edges are materially present.
- C. Selection may affect a turn without becoming identity; identity claims require durable graph evidence, not active-set recurrence alone.
- D. Feedback that lacks a causal explanation should not update durable regard, even if it records a verdict.
- E. Every membrane mutation should remain legible as an operator-auditable event, never as an invisible planner correction.
- F. The TUI boot surface should state the current runtime condition before any style or lore surface.
- G. Static-export mode must always badge itself as non-live and must never imply commit authority.
- H. Every browser mutation control must declare whether it is live-authoritative, preview-only, or static-display-only.
- I. Any geometry metric lacking an explicit evidence class should be omitted from operator-facing interpretation.
- J. If `adam_auto` is a fallback alias rather than a distinct reasoning stage, the UI must say so at the point of selection.
- K. Tanakh payloads must mark canonical text, derived analyzer output, and scene render sidecars as separate provenance classes.

## Intended Use

1. Convert one line into a spec delta only when a concrete code or test touch is planned.
2. Pair each accepted line with one evidence-producing path.
3. Reject any clay patch that cannot be grounded in a specific file, schema, or executable test.
